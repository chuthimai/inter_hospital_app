import 'package:flutter/material.dart';

class AppColors {
  // ===== PRIMARY COLORS =====
  static const Color primaryLight = Color(0xFF456882);
  static const Color primaryDark = Color(0xFF1B3C53);

  // ===== BACKGROUND COLORS =====
  static const Color backgroundLight = Color(0xFFEFEFEF);
  static const Color backgroundDark = Color(0xFF1A2130);

  // ===== TEXT COLORS =====
  static const Color textLight = Color(0xFF000000);
  static const Color textDark = Color(0xFFFFFFFF);

  // ===== SUBTEXT / LABEL COLORS =====
  static const Color subtextLight = Color(0xFF555555);
  static const Color subtextDark = Color(0xFFCCCCCC);

  // ===== CARD / SURFACE COLORS =====
  static const Color cardLight = Color(0xFFFFFFFF);
  static const Color cardDark = Color(0xFF2C2F38);

  // ===== BORDER COLORS =====
  static const Color borderLight = Color(0xFFCCCCCC);
  static const Color borderDark = Color(0xFF444444);

  // ===== ICON COLORS =====
  static const Color iconLight = Color(0xFF000000);
  static const Color iconDark = Color(0xFFFFFFFF);

  // ===== DISABLED COLORS =====
  static const Color disabledLight = Color(0xFFBBBBBB);
  static const Color disabledDark = Color(0xFF666666);

  // ===== SHADOW / ELEVATION COLORS =====
  static const Color shadowLight = Color(0x1F000000); // 12% opacity black
  static const Color shadowDark = Color(0x66000000);  // 40% opacity black

  // ===== ERROR COLORS =====
  static const Color errorLight = Color(0xFFD32F2F);
  static const Color errorDark = Color(0xFFEF9A9A);

  // ===== SUCCESS COLORS =====
  static const Color successLight = Color(0xFF4CAF50);
  static const Color successDark = Color(0xFF81C784);

  // ===== WARNING COLORS =====
  static const Color warningLight = Color(0xFFFFA000);
  static const Color warningDark = Color(0xFFFFD54F);
}
