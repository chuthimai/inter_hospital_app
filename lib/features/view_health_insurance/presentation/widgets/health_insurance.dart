import 'package:flutter/material.dart';

class HealthInsurance extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("HealthInsurance"),
    );
  }

}